# 发现与约束

- 上级工作区已有未提交内容，本项目必须独立隔离，不能改动无关文件。
- 官方 Sites 模板可用于部署，但 Windows 环境下部分脚本需要用 PowerShell 等价流程执行。
- Web Speech API 在浏览器支持时可用于字母和古诗朗读，不支持时按钮保持可用但不会阻塞其他学习功能。
- `localStorage` 是按浏览器和设备保存的，手机扫码后会在手机浏览器里建立自己的学习进度。
- Sites 模板包含 D1 示例代码，本项目未启用数据库绑定，因此相关类型声明保持可选。
